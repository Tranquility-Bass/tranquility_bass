<h1 class="heading"> {{title}} </h1>
<div>
    <h3 class="heading">Discussions</h3>

    {{#if emptyDiscussions}}
        <p>No Discussions Found for that topic</p>
    {{/if}}

    {{#each discussions}}
		<h4 class="heading">
            <a href="/discuss/{{_id}}" class="navLinks">{{title}}</a>
        </h4>
        <p class="allC">{{body}}</p>
    {{/each}}
</div>

<div>
    <h3 class="heading">Reviews</h3>

    {{#if emptyReviews}}
        <p>No Reviews Found for that topic</p>
    {{/if}}

    {{#each reviews}}
		<h4 class="heading">
            <a href="/review/{{_id}}" class="navLinks">{{title}}</a>
        </h4>
        <p class="allC">{{body}}</p>
    {{/each}}
</div>

<br>

<form action="/private/create/discussion/{{searchTerm}}">
	<button type="submit" class="accButton">Create a Discussion Post</button>
</form>
<form action="/private/create/review/{{searchTerm}}">
	<button type="submit" class="accButton">Create a Review Post</button>
</form>

<br>

{{#if isReviewed}}
	<p>User has already reviewed this topic</p>
{{/if}}

<div>
	{{#if song}}
		<h3 class="heading">Artist</h3>
		<a href="/all/{{artistId}}" class="navLinks">{{artist}}</a>
		<h4 class="heading">Album</h4>
		<a href="/all/{{albumId}}" class="navLinks">{{album}}</a>
	{{else}}
		{{#if album}}
			<h3 class="heading">Artist</h3>
			<a href="/all/{{artistId}}" class="navLinks">{{artist}}</a>
			<h4 class="heading">Songs in {{album}}</h4>
			{{#each songs}}
				<a href="/all/{{_id}}" class="navLinks">{{title}}</a>
				<br>
			{{/each}}
		{{else}}
			<h3 class="heading">Albums by {{artist}}</h3>
			{{#each albums}}
				<a href="/all/{{_id}}" class="navLinks">{{title}}</a>
				<br>
			{{/each}}
		{{/if}}
	{{/if}}
	
</div>
    
