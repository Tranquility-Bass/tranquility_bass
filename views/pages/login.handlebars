{{! this gets sent when user is not logged in:

return res.status(403).render('pages/login', {
            title: "Login",
            name: "Login",
            error: "You are not logged in"
        });

}}

<p> {{error}} </p>